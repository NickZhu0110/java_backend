# Maven Support

This directory contains configuration used by the checked-in Maven wrapper.

- [`wrapper`](wrapper/README.md) pins the Maven distribution.
- The launch scripts are `../mvnw` and `../mvnw.cmd`.

See the [backend README](../README.md) for build commands.
