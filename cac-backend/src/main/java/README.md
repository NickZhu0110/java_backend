# Java Source Root

This is the Maven Java source root for the backend.

- [`com`](com/README.md) begins the Java package hierarchy.
- Application classes ultimately reside under `com.cac.backend`.

See the [backend package README](com/cac/backend/README.md).
