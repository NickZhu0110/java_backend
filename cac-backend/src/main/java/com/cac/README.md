# `com.cac` Package Namespace

This directory groups Java code owned by the CAC project.

- [`backend`](backend/README.md) is the Spring Boot application package.

No application classes are defined directly at this namespace level.
