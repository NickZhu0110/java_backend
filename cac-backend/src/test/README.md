# Backend Test Sources

This is the Maven test-source tree for `cac-backend`.

- [`java`](java/README.md) contains JUnit and Spring Boot tests.

Run the suite with `.\mvnw.cmd test` from the backend module.
