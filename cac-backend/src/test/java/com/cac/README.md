# CAC Test Namespace

This directory groups tests for project-owned CAC Java packages.

- [`backend`](backend/README.md) contains the backend context and service tests.

The package hierarchy mirrors `src/main/java/com/cac`.
