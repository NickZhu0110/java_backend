# Documentation

## Purpose

This directory contains supporting operational notes that complement the
project overview in the repository root.

## Key files

- `server-deploy.md` describes the legacy server deployment layout.
- `server-runtime.md` describes the PostgreSQL, Redis, Kafka, backend, and
  worker runtime used outside the Windows-local profile.

## Related directory

See [`../README.md`](../README.md) for the current project entry point.
