# Packaging

## Purpose

This directory contains release-specific source and validation tooling; it is
not a location for generated packages or runtime data.

## Key directory

- [`windows/`](windows/) contains the Windows launcher, fallback launcher,
  package configuration, license collection, and release pipeline.
